extends ../base

block content 
    form(method="POST")
        input(placeholder="Old Password" name="oldPassword" type="password")
        input(placeholder="New Password" name="newPassword" type="password")
        input(placeholder="New Password Confirmation" name="newConfirm" type="password")
        input(type="submit" value="Change assword")